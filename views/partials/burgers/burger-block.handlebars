<li>
    {{name}}
    <button class="eat_burger" data-id="{{id}}" data-devouredState="{{eat}}">
        {{#if eat}}Add Burger{{else}}EAT UP!{{/if}}
    </button>
    <button class="delete_burger" data-id="{{this.id}}">DELETE!</button>
</li>
